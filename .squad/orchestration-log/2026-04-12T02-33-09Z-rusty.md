# Orchestration Log: Rusty — Baggage Fix (2026-04-12T02:33:09Z)

**Agent:** Rusty (Backend Dev)  
**Task:** Fix GetBaggageAsync in MissionApiClient.cs  
**Status:** Completed  

## What Changed

Modified `StirTrekDemo.Web/Services/MissionApiClient.cs`:

- Updated `GetBaggageAsync` to accept `commanderName` and `priority` parameters (with defaults for backward compatibility)
- Start an Activity using `telemetry.ActivitySource.StartActivity("baggage.inspect")`
- Set baggage on the Activity before making the HTTP call:
  - `activity?.SetBaggage("mission.commander", commanderName)` (if non-empty)
  - `activity?.SetBaggage("mission.priority", priority)` (if non-empty)
- This ensures .NET auto-injects the `baggage` header so the API receives baggage context

## Why It Matters

The "Inspect Baggage" feature always returned empty results because bare HTTP requests with no active Activity don't get the baggage header injected by .NET's HttpClient instrumentation. Fixing this unblocks frontend verification of baggage propagation.

## Next Steps

Linus updates the MissionControl.razor component to pass the parameters.

---

**Co-authored-by:** Copilot
